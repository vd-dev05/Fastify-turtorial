Yêu cầu:

Tạo API /register để đăng ký người dùng (hash mật khẩu bằng bcryptjs)

Tạo API /login kiểm tra email và password, nếu hợp lệ thì trả về JWT token

Viết middleware bảo vệ route /profile, chỉ cho phép người có token hợp lệ truy cập